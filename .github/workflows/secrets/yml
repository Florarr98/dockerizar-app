name: secrets-test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Usar secret
        run: |
          echo "El secret es $MY_SECRET"
        env:
          MY_SECRET: ${{ secrets.MY_SECRET }}
